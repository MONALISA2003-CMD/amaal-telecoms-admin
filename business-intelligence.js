export function registerBusinessIntelligence({app,auth,need,q,pool,audit}){
 const n=v=>Number(v||0);
 function range(req){const end=String(req.query.end||new Date().toISOString().slice(0,10));const start=String(req.query.start||new Date(Date.now()-29*86400000).toISOString().slice(0,10));return {start,end}}
 app.get('/api/bi/summary',auth,need('bi.view'),async(req,res)=>{const {start,end}=range(req);const [sales,margin,orders,returns,delivery,warranty,stock,credit]=await Promise.all([
  q('SELECT COALESCE(sum(grand_total),0)::numeric revenue,count(*)::int transactions FROM sales WHERE status=\'Completed\' AND created_at::date BETWEEN $1 AND $2',[start,end]),
  q("SELECT COALESCE(sum((sl.unit_price*sl.quantity)-sl.discount_amount),0)::numeric revenue,COALESCE(sum(sl.cost_price*sl.quantity),0)::numeric cost,COALESCE(sum(((sl.unit_price*sl.quantity)-sl.discount_amount)-(sl.cost_price*sl.quantity)),0)::numeric gross_margin FROM sale_lines sl JOIN sales s ON s.id=sl.sale_id WHERE s.status='Completed' AND s.created_at::date BETWEEN $1 AND $2",[start,end]),
  q("SELECT count(*)::int total,count(*) FILTER(WHERE status='Delivered')::int delivered,count(*) FILTER(WHERE status='Cancelled')::int cancelled FROM orders WHERE created_at::date BETWEEN $1 AND $2",[start,end]),
  q("SELECT count(*)::int total,COALESCE(sum(refund_amount),0)::numeric refunded FROM return_requests WHERE created_at::date BETWEEN $1 AND $2",[start,end]),
  q("SELECT count(*)::int shipments,COALESCE(sum(unit_count),0)::numeric units,COALESCE(sum(total_delivery_cost),0)::numeric cost FROM delivery_shipments WHERE created_at::date BETWEEN $1 AND $2",[start,end]),
  q("SELECT count(*)::int claims,count(*) FILTER(WHERE status IN ('Resolved','Cancelled'))::int closed,COALESCE(avg(EXTRACT(EPOCH FROM (COALESCE((SELECT max(e.created_at) FROM warranty_events e WHERE e.claim_id=w.id AND e.status IN ('Resolved','Cancelled')),now())-w.created_at))/86400),0)::numeric avg_days FROM warranty_claims w WHERE w.created_at::date BETWEEN $1 AND $2",[start,end]),
  q("SELECT COALESCE(sum(on_hand*COALESCE(v.cost_price,0)),0)::numeric stock_value,COALESCE(sum(on_hand),0)::numeric units FROM inventory_balances b JOIN product_variants v ON v.id=b.variant_id",[]),
  q("SELECT COALESCE(sum(outstanding_principal),0)::numeric outstanding,count(*)::int accounts FROM credit_accounts WHERE status IN ('Active','Defaulted','Restructured')",[])
 ]);res.json({range:{start,end},sales:sales[0],margin:margin[0],orders:orders[0],returns:returns[0],delivery:delivery[0],warranty:warranty[0],inventory:stock[0],credit:credit[0],grossMarginPct:n(margin[0].revenue)?n(margin[0].gross_margin)/n(margin[0].revenue)*100:0})});
 app.get('/api/bi/sales-trend',auth,need('bi.view'),async(req,res)=>{const {start,end}=range(req);res.json(await q("SELECT created_at::date day,count(*)::int transactions,COALESCE(sum(grand_total),0)::numeric revenue FROM sales WHERE status='Completed' AND created_at::date BETWEEN $1 AND $2 GROUP BY 1 ORDER BY 1",[start,end]))});
 app.get('/api/bi/products',auth,need('bi.view'),async(req,res)=>{const {start,end}=range(req);res.json(await q("SELECT v.id,v.sku,v.variant_name,p.name product_name,COALESCE(sum(sl.quantity),0)::numeric units,COALESCE(sum(sl.line_total),0)::numeric revenue,COALESCE(sum(sl.cost_price*sl.quantity),0)::numeric cost,COALESCE(sum((sl.line_total)-(sl.cost_price*sl.quantity)),0)::numeric gross_margin FROM sale_lines sl JOIN sales s ON s.id=sl.sale_id JOIN product_variants v ON v.id=sl.variant_id JOIN products p ON p.id=v.product_id WHERE s.status='Completed' AND s.created_at::date BETWEEN $1 AND $2 GROUP BY v.id,v.sku,v.variant_name,p.name ORDER BY revenue DESC LIMIT 200",[start,end]))});
 app.get('/api/bi/inventory-ageing',auth,need('bi.view'),async(req,res)=>res.json(await q(`SELECT v.id,v.sku,p.name product_name,COALESCE(sum(b.on_hand),0)::numeric units,COALESCE(max(m.last_received),NULL) last_received,CASE WHEN max(m.last_received) IS NULL THEN NULL ELSE EXTRACT(DAY FROM now()-max(m.last_received))::int END age_days FROM inventory_balances b JOIN product_variants v ON v.id=b.variant_id JOIN products p ON p.id=v.product_id LEFT JOIN (SELECT variant_id,max(created_at) last_received FROM inventory_movements WHERE movement_type='RECEIPT' GROUP BY variant_id) m ON m.variant_id=v.id GROUP BY v.id,v.sku,p.name HAVING sum(b.on_hand)>0 ORDER BY age_days DESC NULLS LAST LIMIT 300`)));
 app.get('/api/bi/delivery',auth,need('bi.view'),async(req,res)=>res.json(await q(`SELECT COALESCE(p.name,'Unassigned') partner,count(*)::int shipments,COALESCE(sum(s.unit_count),0)::numeric units,COALESCE(sum(s.total_delivery_cost),0)::numeric total_cost,CASE WHEN sum(s.unit_count)>0 THEN (sum(s.total_delivery_cost)/sum(s.unit_count))::numeric ELSE 0 END unit_cost FROM delivery_shipments s LEFT JOIN delivery_partners p ON p.id=s.partner_id GROUP BY p.name ORDER BY total_cost DESC`)));
 app.get('/api/bi/warranty',auth,need('bi.view'),async(req,res)=>res.json(await q(`SELECT COALESCE(p.name,'Internal') repair_partner,count(*)::int jobs,count(*) FILTER(WHERE j.status NOT IN ('Completed','Cancelled'))::int active_jobs,count(*) FILTER(WHERE j.status='Completed')::int completed_jobs,COALESCE(sum(j.partner_cost),0)::numeric partner_cost,COALESCE(avg(EXTRACT(EPOCH FROM (COALESCE(j.completed_at,now())-j.created_at))/86400),0)::numeric avg_days FROM repair_jobs j LEFT JOIN repair_partners p ON p.id=j.partner_id GROUP BY p.name ORDER BY active_jobs DESC,partner_cost DESC`)));
 app.get('/api/bi/export',auth,need('bi.export'),async(req,res)=>{const {start,end}=range(req);const rows=await q("SELECT created_at::date day,count(*) transactions,COALESCE(sum(grand_total),0)::numeric revenue FROM sales WHERE status='Completed' AND created_at::date BETWEEN $1 AND $2 GROUP BY 1 ORDER BY 1",[start,end]);const csv=['date,transactions,revenue',...rows.map(r=>`${r.day},${r.transactions},${r.revenue}`)].join('\n');res.setHeader('Content-Type','text/csv; charset=utf-8');res.setHeader('Content-Disposition',`attachment; filename="amaal-bi-${start}-to-${end}.csv"`);res.send(csv)});
}
