import Link from 'next/link';import SiteHeader from './SiteHeader';import SiteFooter from './SiteFooter';import WishlistButton from './WishlistButton';
type P={name:string;brand:string;description:string;quickSpecs:string[];specs:Record<string,string>;type?:string;family?:string;segment?:string};
export default function CuratedProductDetail({product,backHref,backLabel}:{product:P;backHref:string;backLabel:string}){return <main><SiteHeader/><section className="section product-detail-clean"><Link className="backLink" href={backHref}>← {backLabel}</Link><div className="detail-layout"><div className="detail-media-placeholder"><span>{product.brand}</span><strong>Product image unavailable</strong><small>Amaal will publish the product image when the item is ready for live sale.</small></div><div className="detail-copy"><p className="eyebrow">{product.brand}{product.family?` · ${product.family}`:''}</p><div className="detail-title-row"><h1>{product.name}</h1><WishlistButton id={product.name}/></div><p className="detail-lead">{product.description}</p><div className="detail-price">Price coming soon</div><Link className="button gold" href={`/contact?product=${encodeURIComponent(product.name)}`}>Ask about this product</Link><div className="detail-note"><strong>Availability & configuration</strong><span>Storage, colour, connectivity and other configurations can vary by model. Amaal confirms the exact configuration before purchase.</span></div></div></div><section className="detail-section"><div><p className="eyebrow">PRODUCT INFORMATION</p><h2>What to know.</h2></div><div className="detail-info-grid"><article><h3>Key features</h3><div className="feature-list">{product.quickSpecs.map(s=><span key={s}>✓ {s}</span>)}</div></article><article><h3>Specifications</h3><dl>{Object.entries(product.specs).map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v}</dd></div>)}</dl></article></div></section></section><SiteFooter/></main>}
