'use client';
import {ShoppingBag} from 'lucide-react';import {useState} from 'react';import {addCart} from '../lib/commerce';
type Props={id:string;name:string;price:string;numericPrice?:number;variantId?:string;brand?:string;slug?:string};
export default function AddToBag({id,name,price,numericPrice,variantId,brand,slug}:Props){const [added,setAdded]=useState(false);const canBuy=Number(numericPrice||0)>0;if(!canBuy)return <button className="add-button muted" disabled title="Online purchase will be enabled when pricing is published"><ShoppingBag size={15}/>Price coming soon</button>;function add(){addCart({id,name,price:numericPrice||0,variantId,brand,slug});setAdded(true);setTimeout(()=>setAdded(false),1400)}return <button className="add-button" onClick={add}><ShoppingBag size={15}/>{added?'Added to bag':'Add to bag'}</button>}
