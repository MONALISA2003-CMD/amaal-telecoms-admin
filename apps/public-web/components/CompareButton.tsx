'use client';import {Scale} from 'lucide-react';import {useEffect,useState} from 'react';import Link from 'next/link';import {getCompare,toggleCompare} from '../lib/compare';export default function CompareButton({id}:{id:string}){const [on,setOn]=useState(false);useEffect(()=>setOn(getCompare().includes(id)),[id]);return <span className="compare-control"><button className={on?'selected':''} onClick={()=>setOn(toggleCompare(id).includes(id))}><Scale size={14}/>{on?'Compared':'Compare'}</button>{getCompare().length>1&&<Link href="/compare">View compare</Link>}</span>}
