'use client';
import Link from 'next/link';import {ArrowRight} from 'lucide-react';import {useEffect,useState} from 'react';
const fallback=[['Everyday essentials','The devices and appliances that make daily routines easier.','/shop'],['Work & study','Laptops, tablets, phones and accessories for getting things done.','/categories/computers/laptops'],['Home entertainment','TVs and audio for better evenings at home.','/categories/entertainment/audio'],['Connected home','Refrigeration, laundry, cooling and smart-home ready appliances.','/categories/home-appliances'],['Kitchen, made simpler','Cooking, preparation and beverage appliances for everyday life.','/categories/kitchen-appliances'],['Gaming & performance','Performance laptops and the accessories around them.','/categories/computers/laptops/gaming']];
export default function CollectionsClient(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{const base=process.env.NEXT_PUBLIC_API_BASE_URL||process.env.AMAAL_API_BASE_URL||'';fetch(`${base}/api/public/catalog`).then(r=>r.ok?r.json():null).then(d=>setRows((d?.collections||[]).filter((x:any)=>x.products?.length||x.description).slice(0,12))).catch(()=>{})},[]);const cards=rows.length?rows.map(x=>[x.name,x.description||'A curated selection from Amaal.',`/collections/${x.slug}`] as const):fallback;return <div className="collection-grid">{cards.map(([title,text,href],i)=><Link href={href} className="collection-card" key={title}><span>{String(i+1).padStart(2,'0')}</span><h2>{title}</h2><p>{text}</p><b>Explore collection <ArrowRight size={15}/></b></Link>)}</div>}
