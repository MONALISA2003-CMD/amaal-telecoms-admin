'use client';
import Link from 'next/link';import {useEffect,useState} from 'react';import {ArrowRight,Sparkles} from 'lucide-react';
export default function RecommendationRail({productId}:{productId:string}){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{const base=process.env.NEXT_PUBLIC_API_BASE_URL||process.env.AMAAL_API_BASE_URL||'';fetch(`${base}/api/public/recommendations?productId=${encodeURIComponent(productId)}&limit=8`).then(r=>r.ok?r.json():null).then(d=>setRows(d?.rows||[])).catch(()=>{})},[productId]);if(!rows.length)return null;return <section className="section recommendation-rail"><div className="section-head"><div><p className="eyebrow"><Sparkles size={14}/> CURATED FOR YOU</p><h2>Products worth considering.</h2></div><Link href="/shop">View shop <ArrowRight size={15}/></Link></div><div className="recommendation-grid">{rows.map(x=><Link className="recommendation-card" key={`${x.id}-${x.relation_type}`} href={`/product/${x.slug}`}><div className="recommendation-media"><span>AMAAL</span></div><small>{x.relation_type}</small><strong>{x.brand_name||'AMAAL'}</strong><h3>{x.name}</h3></Link>)}</div></section>}
