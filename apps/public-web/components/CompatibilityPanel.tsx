'use client';
import Link from 'next/link';import {Cable,ChevronRight} from 'lucide-react';import {useEffect,useState} from 'react';
export default function CompatibilityPanel({slug}:{slug:string}){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch(`${process.env.NEXT_PUBLIC_API_BASE_URL||process.env.AMAAL_API_BASE_URL||''}/api/public/products/${slug}/compatible`).then(r=>r.ok?r.json():null).then(d=>setRows(d?.rows||[])).catch(()=>{})},[slug]);if(!rows.length)return null;return <section className="section compatibility-panel"><div className="section-head"><div><p className="eyebrow">COMPATIBILITY</p><h2>Products that fit together.</h2></div><p>Amaal shows compatibility rules maintained in Business Console, not guesses from similar names.</p></div><div className="compatibility-grid">{rows.map(r=><Link key={r.id} className="compatibility-card" href={`/product/${r.slug}`}><div>{r.image_url?<div className="compatibility-image" style={{backgroundImage:`url(${r.image_url})`}}/>:<div className="compatibility-image fallback"><Cable size={18}/></div>}</div><div><small>{r.relation_type} · {r.confidence}</small><h3>{r.name}</h3><p>{r.notes||'Verified compatibility information.'}</p></div><ChevronRight size={16}/></Link>)}</div></section>}
