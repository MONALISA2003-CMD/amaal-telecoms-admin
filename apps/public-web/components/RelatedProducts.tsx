'use client';
import Link from 'next/link';import {ArrowRight,PackagePlus} from 'lucide-react';import {useEffect,useState} from 'react';
export default function RelatedProducts({slug}:{slug:string}){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{const base=process.env.NEXT_PUBLIC_API_BASE_URL||process.env.AMAAL_API_BASE_URL||'';fetch(`${base}/api/public/products/${encodeURIComponent(slug)}/related`).then(r=>r.ok?r.json():null).then(d=>setRows(d?.rows||[])).catch(()=>{})},[slug]);if(!rows.length)return null;return <section className="section related-products"><div className="section-head"><div><p className="eyebrow">COMPLETE THE SETUP</p><h2>Products that work well together.</h2></div><PackagePlus size={24}/></div><div className="related-grid">{rows.slice(0,8).map(x=><Link href={`/product/${x.slug}`} className="related-card" key={`${x.id}-${x.relation_type}`}><div className="related-media"><span>AMAAL</span><small>Product image</small></div><p>{x.brand_name}</p><h3>{x.name}</h3><span>{x.relation_type==='Upsell'?'Consider an upgrade':x.relation_type==='Cross-sell'?'Often bought together':'Related product'} <ArrowRight size={14}/></span></Link>)}</div></section>}
