import Link from 'next/link';import { ArrowRight, Sparkles } from 'lucide-react';import SiteHeader from '../../components/SiteHeader';import SiteFooter from '../../components/SiteFooter';import TVCatalogueClient from '../../components/TVCatalogueClient';import {tvCatalogue,tvMasterBrands} from '../../lib/tv-catalogue';
export default function TVsPage(){const featured=tvCatalogue.filter(p=>p.generation==='CURRENT').slice(0,8);return <main><SiteHeader/><section className="tv-hero tv-hero-sun"><div className="tv-hero-copy"><p className="eyebrow">AMAAL · TV & ENTERTAINMENT</p><h1>Find your<br/><em>perfect screen.</em></h1><p>Explore televisions by screen size, then discover the models from each trusted brand.</p><div className="tv-hero-actions"><a className="button gold" href="#catalogue">Browse televisions <ArrowRight size={15}/></a><Link className="button ghost-light" href="/shop">Shop everything</Link></div></div><div className="tv-hero-stage"><div className="tv-hero-placeholder"><span>HERO MEDIA</span><strong>Premium home entertainment</strong><small>Premium product photography coming soon.</small></div></div></section><section className="tv-trust"><div><strong>Genuine products</strong><span>Carefully selected brands</span></div><div><strong>Multiple sizes</strong><span>Choose the right screen</span></div><div><strong>Uganda delivery</strong><span>Convenient local service</span></div><div><strong>Real support</strong><span>Help before and after purchase</span></div></section><section className="section tv-brands"><div className="section-head"><div><p className="eyebrow">SHOP BY BRAND</p><h2>Trusted names. Beautiful screens.</h2><p className="section-intro">Start with a brand or jump straight to a screen size.</p></div></div><div className="tv-brand-grid">{tvMasterBrands.map(b=><Link key={b} href={`/tvs/brand/${b.toLowerCase().replace(/[^a-z0-9]+/g,'-')}`}><span>{b}</span><small>{tvCatalogue.filter(p=>p.brand===b).length} models</small><ArrowRight size={15}/></Link>)}</div></section><section className="section tv-featured"><div className="section-head"><div><p className="eyebrow">FEATURED</p><h2>Made for the moment.</h2></div><Sparkles size={22}/></div><div className="tv-mini-grid">{featured.map(p=><Link href={`/tvs/${p.slug}`} key={p.slug}><div className="tv-mini-art">{p.slug==='samsung-u8000h'?<img src="/products/samsung-u8000f-tv.webp" alt={`${p.brand} ${p.model}`}/>:<span>AMAAL</span>}</div><span>{p.brand}</span><strong>{p.model}</strong></Link>)}</div></section><section id="catalogue" className="section tv-catalogue"><div className="section-head"><div><p className="eyebrow">FULL COLLECTION</p><h2>Every model, clearly presented.</h2><p className="section-intro">Search, filter and open any model to see its available screen sizes and product information.</p></div></div><TVCatalogueClient products={tvCatalogue} brands={tvMasterBrands}/></section><SiteFooter/></main>}
