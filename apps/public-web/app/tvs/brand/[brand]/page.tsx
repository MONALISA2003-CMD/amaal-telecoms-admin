import {notFound} from 'next/navigation';import Link from 'next/link';import SiteHeader from '../../../../components/SiteHeader';import SiteFooter from '../../../../components/SiteFooter';import TVCatalogueClient from '../../../../components/TVCatalogueClient';import {tvBrands,tvCatalogue,tvSlug} from '../../../../lib/tv-catalogue';
export function generateStaticParams(){return tvBrands.map(b=>({brand:tvSlug(b,'')}));}
export default async function TVBrandPage({params}:{params:Promise<{brand:string}>}){const {brand}=await params;const name=tvBrands.find(b=>tvSlug(b,'')===brand);if(!name)notFound();const products=tvCatalogue.filter(p=>p.brand===name);return <main><SiteHeader/><section className="section tv-brand-hero"><p className="eyebrow">AMAAL · {name.toUpperCase()}</p><h1>{name}<br/><em>televisions.</em></h1><p>Explore the {name} models available through Amaal, with screen-size choices kept together.</p><Link className="quiet-link" href="/tvs">← All televisions</Link></section><section className="section tv-catalogue tv-brand-catalogue"><TVCatalogueClient products={products} brands={[name]}/></section><SiteFooter/></main>}
