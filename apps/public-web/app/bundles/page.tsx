'use client';
import Link from 'next/link';import {useEffect,useState} from 'react';import SiteHeader from '../../components/SiteHeader';import SiteFooter from '../../components/SiteFooter';import {PackageOpen} from 'lucide-react';
export default function Bundles(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{const base=process.env.NEXT_PUBLIC_API_BASE_URL||process.env.AMAAL_API_BASE_URL||'';fetch(`${base}/api/public/bundles`).then(r=>r.ok?r.json():null).then(d=>setRows(d?.rows||[])).catch(()=>{})},[]);return <main><SiteHeader/><section className="section bundles-page"><p className="eyebrow">AMAAL BUNDLES</p><h1>Buy together, when it makes sense.</h1><p className="listing-lead">Curated combinations published by Amaal. Bundle pricing is only shown when it is configured and approved.</p>{rows.length?<div className="bundle-grid">{rows.map(b=><article className="bundle-card" key={b.id}><PackageOpen/><h2>{b.name}</h2><p>{b.description||'A considered combination of compatible Amaal products.'}</p><div>{b.items?.slice(0,5).map((i:any)=><Link key={i.variantId} href={`/product/${i.productSlug}`}>{i.name}{i.variantName?` · ${i.variantName}`:''}</Link>)}</div><span>{b.discount_type==='Percent'?`${b.discount_value}% bundle saving`:b.discount_type==='Fixed'?`UGX ${Number(b.discount_value).toLocaleString('en-UG')} bundle saving`:'Bundle pricing'}</span></article>)}</div>:<div className="empty"><PackageOpen/><h2>No bundles are published yet.</h2><Link className="button gold" href="/shop">Explore the catalogue</Link></div>}</section><SiteFooter/></main>}
