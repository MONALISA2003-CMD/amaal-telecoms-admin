import SiteHeader from '../../../components/SiteHeader';import SiteFooter from '../../../components/SiteFooter';import DatabaseCompareClient from '../../../components/DatabaseCompareClient';import {getPublishedCatalog} from '../../../lib/catalog-runtime';
export default async function PhoneComparePage(){const c=await getPublishedCatalog();const products=c.products.filter(p=>p.category_slug?.startsWith('phones'));return <main><SiteHeader/><section className="section listing-page"><p className="eyebrow">PHONE COMPARISON</p><h1>Compare phones clearly.</h1><DatabaseCompareClient products={products}/></section><SiteFooter/></main>}
