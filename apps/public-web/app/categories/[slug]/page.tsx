import Link from 'next/link';
import { getCatalog } from '../../../lib/catalog';
export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const catalog=await getCatalog(); const name=slug.replace(/-/g,' ').replace(/\b\w/g,c=>c.toUpperCase()); const products=(catalog?.products??[]).filter((p:any)=>{const text=JSON.stringify(p).toLowerCase(); return text.includes(slug.replace(/-/g,' ')) || text.includes(slug);}); return <main><header className="site-header"><div className="nav-wrap"><Link className="brand" href="/">AMAAL<span>.</span></Link><Link className="back-link" href="/categories">All categories</Link></div></header><section className="section listing-page"><p className="eyebrow">CATEGORY</p><h1>{name}.</h1><p>Explore Amaal's {name.toLowerCase()} collection.</p><div className="listing-toolbar"><span>{products.length} matching products</span><Link href="/search">Search</Link></div><div className="product-grid">{products.map((p:any)=><Link className="product-card" href={`/product/${p.slug??p.id}`} key={p.id??p.slug}><div className="product-image">{p.image_url?<img src={p.image_url} alt=""/>:<span>AMAAL</span>}</div><p className="product-brand">{p.brand_name??p.brand??'AMAAL'}</p><h3>{p.name}</h3><strong>{p.price?`UGX ${Number(p.price).toLocaleString()}`:'View product'}</strong></Link>)}</div>{products.length===0&&<div className="empty">We couldn't find published products in this category yet.</div>}</section></main>}
