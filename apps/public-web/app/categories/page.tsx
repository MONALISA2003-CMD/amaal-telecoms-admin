import Link from 'next/link';
import SiteHeader from '../../components/SiteHeader';import SiteFooter from '../../components/SiteFooter';
import {amaalCategoryNavigation} from '../../lib/category-navigation';
export default function CategoriesPage(){return <main><SiteHeader/><section className="section category-directory"><div className="listing-intro"><p className="eyebrow">SHOP Amaal</p><h1>Everything, organized.</h1><p>Browse Amaal by category, then narrow down by brand, family, model and configuration.</p></div><div className="category-directory-grid">{amaalCategoryNavigation.map(c=><article className="category-directory-card" key={c.slug}><Link href={c.href} className="category-directory-main"><span className="category-index">{String(amaalCategoryNavigation.indexOf(c)+1).padStart(2,'0')}</span><div><h2>{c.name}</h2><p>{c.description}</p></div><b>Explore →</b></Link>{c.children&&<div className="category-child-grid">{c.children.map(ch=><Link href={ch.href} key={ch.slug}><strong>{ch.name}</strong><span>{ch.description}</span></Link>)}</div>}</article>)}</div></section><SiteFooter/></main>}
