import Link from 'next/link';import SiteHeader from '../../components/SiteHeader';import SiteFooter from '../../components/SiteFooter';import {amaalCategoryNavigation,CategoryNavNode} from '../../lib/category-navigation';
function ChildTree({nodes,depth=0}:{nodes:CategoryNavNode[];depth?:number}){return <div className={`category-child-grid depth-${depth}`}>{nodes.map(ch=><div className="category-child-wrap" key={ch.href+ch.slug}><Link href={ch.href}><strong>{ch.name}</strong><span>{ch.description}</span></Link>{ch.children&&<ChildTree nodes={ch.children} depth={depth+1}/>}</div>)}</div>}
export default function CategoriesPage(){return <main><SiteHeader/><section className="section category-directory"><div className="category-directory-hero"><div className="listing-intro"><p className="eyebrow">SHOP AMAAL</p><h1>Everything, organized.</h1><p>Browse Amaal by category, then narrow down by brand, family, model and configuration. Product photos and prices are added when products are ready for live sale.</p></div></div><div className="category-directory-grid">{amaalCategoryNavigation.map((c,i)=><article className="category-directory-card" key={c.slug}><Link href={c.href} className="category-directory-main"><span className="category-index">{String(i+1).padStart(2,'0')}</span><div><h2>{c.name}</h2><p>{c.description}</p></div><b>Explore →</b></Link>{c.children&&<ChildTree nodes={c.children}/>}</article>)}</div></section><SiteFooter/></main>}
