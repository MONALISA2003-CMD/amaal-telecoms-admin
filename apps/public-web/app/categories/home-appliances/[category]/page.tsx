import Link from 'next/link';import {notFound} from 'next/navigation';import SiteHeader from '../../../../components/SiteHeader';import SiteFooter from '../../../../components/SiteFooter';import UnifiedCatalogueClient from '../../../../components/UnifiedCatalogueClient';import {homeApplianceProducts,homeApplianceCategories} from '../../../../lib/appliance-catalogue';
export default async function HomeApplianceCategoryPage({params}:{params:Promise<{category:string}>}){const {category}=await params;const title=homeApplianceCategories.find(c=>c.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')===category);if(!title)notFound();const items=homeApplianceProducts.filter(p=>p.category===title);return <main><SiteHeader/><section className="section listing-page"><Link className="backLink" href="/categories/home-appliances">← Home Appliances</Link><p className="eyebrow">HOME APPLIANCES</p><h1>{title}.</h1><p className="listingLead">Compare verified catalogue entries by brand and product format.</p><UnifiedCatalogueClient items={items} basePath="/categories/home-appliances/product" searchPlaceholder={`Search ${title.toLowerCase()}…`} filterLabel="products" filterKey="family"/></section><SiteFooter/></main>}
