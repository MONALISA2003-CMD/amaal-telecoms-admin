import Link from 'next/link';import {notFound} from 'next/navigation';import SiteHeader from '../../../../../components/SiteHeader';import SiteFooter from '../../../../../components/SiteFooter';import {homeApplianceProducts} from '../../../../../lib/appliance-catalogue';
export default async function HomeApplianceProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=homeApplianceProducts.find(x=>x.slug===slug);if(!p)notFound();return <main><SiteHeader/><section className="section product-detail"><div className="detail-media"><div className="detail-image"><div className="detail-placeholder"><span>AMAAL</span><strong>PRODUCT PHOTO</strong><small>Product photo coming soon</small></div></div></div><div className="detail-copy"><Link className="backLink" href="/categories/home-appliances">← Home Appliances</Link><p className="eyebrow">{p.brand} · {p.category}</p><h1>{p.name}</h1><div className="detail-price">Price coming soon</div><p className="detail-note">{p.description}</p><div className="quick-specs">{p.quickSpecs.map(x=><span key={x}>{x}</span>)}</div><div className="product-detail-more"><p className="eyebrow">PRODUCT INFORMATION</p><h2>Product details.</h2><div className="tv-specs">{Object.entries(p.specs).map(([k,v])=><div key={k}><strong>{k}</strong><span>{v}</span></div>)}</div></div></div></section><SiteFooter/></main>}
