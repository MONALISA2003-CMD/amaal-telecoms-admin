import SiteHeader from '@/components/SiteHeader';import SiteFooter from '@/components/SiteFooter';import DatabaseCatalogueClient from '@/components/DatabaseCatalogueClient';import {getPublishedCatalog,productsForCategory} from '@/lib/catalog-runtime';
export default async function Page({params}:{params:Promise<{category:string}>}){const catalog=await getPublishedCatalog();const p=await params;const products=productsForCategory(catalog,[p.category,'kitchen-appliances']);const heading=products[0]?.category_name||p.category;return <main><SiteHeader/><section className="section listing-page"><p className="eyebrow">AMAAL CATALOGUE</p><h1>{heading}.</h1><p>Explore published products from Amaal.</p><DatabaseCatalogueClient items={products} title={heading}/></section><SiteFooter/></main>}