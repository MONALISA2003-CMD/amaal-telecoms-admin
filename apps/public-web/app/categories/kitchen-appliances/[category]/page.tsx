import Link from 'next/link';import {notFound} from 'next/navigation';import SiteHeader from '../../../../components/SiteHeader';import SiteFooter from '../../../../components/SiteFooter';import UnifiedCatalogueClient from '../../../../components/UnifiedCatalogueClient';import {kitchenApplianceProducts,kitchenApplianceCategories} from '../../../../lib/appliance-catalogue';
export default async function KitchenApplianceCategoryPage({params}:{params:Promise<{category:string}>}){const {category}=await params;const title=kitchenApplianceCategories.find(c=>c.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')===category);if(!title)notFound();const items=kitchenApplianceProducts.filter(p=>p.category===title);return <main><SiteHeader/><section className="section listing-page"><Link className="backLink" href="/categories/kitchen-appliances">← Kitchen Appliances</Link><p className="eyebrow">KITCHEN APPLIANCES</p><h1>{title}.</h1><p className="listingLead">Compare verified catalogue entries by brand and product format.</p><UnifiedCatalogueClient items={items} basePath="/categories/kitchen-appliances/product" searchPlaceholder={`Search ${title.toLowerCase()}…`} filterLabel="products" filterKey="family"/></section><SiteFooter/></main>}
