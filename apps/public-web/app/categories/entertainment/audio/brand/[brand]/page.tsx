import {notFound} from 'next/navigation';import SiteHeader from '@/components/SiteHeader';import SiteFooter from '@/components/SiteFooter';import DatabaseCatalogueClient from '@/components/DatabaseCatalogueClient';import {getPublishedCatalog} from '@/lib/catalog-runtime';
export default async function Page({params}:{params:Promise<{brand:string}>}){const {brand}=await params;const c=await getPublishedCatalog();const b=c.brands.find(x=>x.slug===brand);const products=c.products.filter(p=>p.brand_slug===brand);if(!b&&!products.length)notFound();const heading=b?.name||brand.replace(/-/g,' ');return <main><SiteHeader/><section className="section listing-page"><p className="eyebrow">BRAND</p><h1>{heading}.</h1><p>{b?.description||`Explore ${heading} products available from Amaal.`}</p><DatabaseCatalogueClient items={products} title={heading}/></section><SiteFooter/></main>}