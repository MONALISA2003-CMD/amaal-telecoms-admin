import {redirect} from 'next/navigation';export default async function AudioBrandRedirect({params}:{params:Promise<{brand:string}>}){const {brand}=await params;redirect(`/categories/entertainment/audio/brand/${encodeURIComponent(brand)}`)}
