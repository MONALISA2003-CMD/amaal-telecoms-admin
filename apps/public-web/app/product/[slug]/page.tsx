import {notFound} from 'next/navigation';import SiteHeader from '@/components/SiteHeader';import SiteFooter from '@/components/SiteFooter';import DatabaseProductDetail from '@/components/DatabaseProductDetail';import {getPublishedCatalog} from '@/lib/catalog-runtime';
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=await getPublishedCatalog();const p=c.products.find(x=>x.slug===slug);if(!p)notFound();return <main><SiteHeader/><DatabaseProductDetail product={p}/><SiteFooter/></main>}
