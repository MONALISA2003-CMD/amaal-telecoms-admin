import Link from 'next/link';import {Search} from 'lucide-react';import SiteHeader from '../../components/SiteHeader';import SiteFooter from '../../components/SiteFooter';import ProductCard from '../../components/ProductCard';import {getCatalog,type Product} from '../../lib/catalog';
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=''}=await searchParams;const query=q.trim().toLowerCase();const catalog=await getCatalog();const products=(catalog?.products??[]).filter((p:Product)=>!query||`${p.name} ${p.brand_name??''} ${p.category_name??''}`.toLowerCase().includes(query));return <main><SiteHeader/><section className="section search-page"><p className="eyebrow">SEARCH AMAAL</p><h1>Find what you need.</h1><form className="search-form"><Search size={19}/><input name="q" defaultValue={q} placeholder="Search phones, TVs, fridges, speakers…" aria-label="Search products"/><button>Search</button></form><div className="section-head results-head"><h2>{query?`${products.length} result${products.length===1?'':'s'} for “${q}”`:'Browse our catalogue'}</h2></div><div className="product-grid">{products.slice(0,24).map(p=><ProductCard key={p.id} product={p}/>)}</div>{products.length===0&&<div className="empty">No products matched your search. Try another term or browse categories.</div>}</section><SiteFooter/></main>}
