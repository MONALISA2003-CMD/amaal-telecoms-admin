import SiteHeader from '../../components/SiteHeader';import SiteFooter from '../../components/SiteFooter';import UniversalShopClient from '../../components/UniversalShopClient';import {getCatalog} from '../../lib/catalog';
export default async function ShopPage(){const live=await getCatalog();const items=live?.products||[];return <main><SiteHeader/><section className="section shop-page"><div className="shop-hero"><p className="eyebrow">AMAAL SHOP</p><h1>Find what fits your life.</h1><p>Explore Amaal's published catalogue across phones, tablets, computing, entertainment, appliances and accessories.</p></div><UniversalShopClient items={items}/></section><SiteFooter/></main>}
