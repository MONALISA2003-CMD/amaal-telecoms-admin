import Link from 'next/link';import SiteHeader from '../../components/SiteHeader';import SiteFooter from '../../components/SiteFooter';import {getPublishedCatalog} from '../../lib/catalog-runtime';
export default async function BrandsPage(){const c=await getPublishedCatalog();return <main><SiteHeader/><section className="section listing-page"><p className="eyebrow">BRANDS</p><h1>Trusted names.</h1><p>Browse brands with products currently published in the Amaal catalogue.</p><div className="brand-grid">{c.brands.map(b=><Link href={`/brands/${b.slug}`} key={b.id}><strong>{b.name}</strong><span>{c.products.filter(p=>p.brand_slug===b.slug).length} products</span></Link>)}</div></section><SiteFooter/></main>}