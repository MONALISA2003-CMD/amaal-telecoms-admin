import {redirect} from 'next/navigation';export default async function ComputerProductRedirect({params}:{params:Promise<{slug:string}>}){const {slug}=await params;redirect(`/categories/computers/laptops/${slug}`)}
