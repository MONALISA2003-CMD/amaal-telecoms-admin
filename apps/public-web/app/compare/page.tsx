'use client';import Link from 'next/link';import {useEffect,useState} from 'react';import SiteHeader from '../../components/SiteHeader';import SiteFooter from '../../components/SiteFooter';import {getCompare,clearCompare} from '../../lib/compare';
export default function Compare(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{const i=getCompare();const base=process.env.NEXT_PUBLIC_API_BASE_URL||process.env.AMAAL_API_BASE_URL||'';fetch(`${base}/api/public/catalog`).then(r=>r.ok?r.json():null).then(d=>setRows((d?.products||[]).filter((p:any)=>i.includes(String(p.id))))).catch(()=>{})},[]);return <main><SiteHeader/><section className="section compare-page"><p className="eyebrow">COMPARE</p><h1>Put your shortlist side by side.</h1><p className="listing-lead">Compare up to four catalogue products. Final pricing and availability are confirmed by Amaal before purchase.</p>{rows.length?<><div className="compare-table-wrap"><table><thead><tr><th>Product</th>{rows.map(p=><th key={p.id}>{p.name}</th>)}</tr></thead><tbody><tr><th>Brand</th>{rows.map(p=><td key={p.id}>{p.brand_name||'—'}</td>)}</tr><tr><th>Category</th>{rows.map(p=><td key={p.id}>{p.category_name||'—'}</td>)}</tr><tr><th>Price</th>{rows.map(p=><td key={p.id}>Price coming soon</td>)}</tr><tr><th>Description</th>{rows.map(p=><td key={p.id}>{p.short_description||p.description||'Details to be confirmed.'}</td>)}</tr></tbody></table></div><button className="button outline" onClick={()=>{clearCompare();setRows([])}}>Clear comparison</button></>:<div className="empty"><h2>Your comparison is empty.</h2><p>Choose Compare on products you are considering.</p><Link className="button gold" href="/shop">Browse products</Link></div>}</section><SiteFooter/></main>}
