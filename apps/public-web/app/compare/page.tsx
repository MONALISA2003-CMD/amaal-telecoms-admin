import SiteHeader from '../../components/SiteHeader';import SiteFooter from '../../components/SiteFooter';import DatabaseCompareClient from '../../components/DatabaseCompareClient';import {getPublishedCatalog} from '../../lib/catalog-runtime';export default async function ComparePage(){const c=await getPublishedCatalog();return <main><SiteHeader/><section className="section listing-page"><p className="eyebrow">COMPARE</p><h1>Compare products clearly.</h1><p>Compare products from Amaal's published catalogue using the information available for each item.</p><DatabaseCompareClient products={c.products}/></section><SiteFooter/></main>}