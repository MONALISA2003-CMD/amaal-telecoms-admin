'use client';import {useState} from 'react';import Link from 'next/link';import SiteHeader from '../../components/SiteHeader';import SiteFooter from '../../components/SiteFooter';
export default function Contact(){const [f,setF]=useState({name:'',phone:'',email:'',title:'',description:''});const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);async function submit(e:any){e.preventDefault();setBusy(true);setMessage('');try{const base=process.env.NEXT_PUBLIC_API_BASE_URL||process.env.AMAAL_API_BASE_URL||'';const r=await fetch(`${base}/api/public/support`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'We could not send your request.');setMessage(`Your request ${d.case?.case_no||''} has been received. Amaal will follow up.`);setF({name:'',phone:'',email:'',title:'',description:''})}catch(e:any){setMessage(e.message)}finally{setBusy(false)}}return <main><SiteHeader/><section className="section contact-page"><p className="eyebrow">CONTACT AMAAL</p><h1>How can we help?</h1><p className="listing-lead">For product enquiries, availability, delivery, partnerships or support, send the Amaal team a message.</p><div className="contact-layout"><form className="checkout-panel" onSubmit={submit}><div className="form-grid"><label>Full name<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Phone number<input required value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></label><label>Email <span>optional</span><input type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>Subject<input required value={f.title} onChange={e=>setF({...f,title:e.target.value})}/></label><label className="full">Message<textarea required rows={7} value={f.description} onChange={e=>setF({...f,description:e.target.value})}/></label></div><button className="button gold" disabled={busy}>{busy?'Sending…':'Send message'}</button>{message&&<p className="form-message">{message}</p>}</form><aside className="contact-card"><h2>Other ways to reach us.</h2><p>For urgent order questions, keep your order number and checkout phone nearby.</p><Link href="/tracking">Track an order →</Link><Link href="/faq">Read common questions →</Link><Link href="/services">Open customer care →</Link></aside></div></section><SiteFooter/></main>}
