import Link from 'next/link';import SiteHeader from '../../../components/SiteHeader';import SiteFooter from '../../../components/SiteFooter';import {getCatalog} from '../../../lib/catalog';import ProductCard from '../../../components/ProductCard';
export default async function CollectionPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=await getCatalog();const row=(c?.collections||[]).find((x:any)=>x.slug===slug);if(!row)return <main><SiteHeader/><section className="section empty"><h1>Collection not found.</h1><Link href="/collections">Back to collections</Link></section><SiteFooter/></main>;return <main><SiteHeader/><section className="section listing-page"><p className="eyebrow">COLLECTION</p><h1>{row.name}</h1><p className="listing-lead">{row.description}</p>{row.products?.length?<div className="product-grid">{row.products.map((p:any)=><ProductCard key={p.id} product={{id:p.id,name:p.name,slug:p.slug,brand_name:p.brand,category_name:p.category,short_description:''} as any}/>)}</div>:<div className="empty"><h2>This collection is being prepared.</h2><p>Check back soon for the latest selection from Amaal.</p></div>}</section><SiteFooter/></main>}
