import InfoPage from '../../components/InfoPage';export default function Returns(){return <InfoPage eyebrow="RETURNS" title="Returns made clear." lead="Amaal’s return process is designed around clear eligibility, review and status updates." items={[["Eligibility","Return eligibility depends on the product, order and applicable Amaal policy."],["Request","Provide the order and product details so the request can be reviewed."],["Status","Approved requests should remain trackable through the customer support journey."]]}/>}
