Continue Amaal Telecoms Admin from the supplied ZIP. Treat HARDENING-AUDIT-1-20.md as authoritative. Do not create Git branches, do not add YAML workflow files, and do not reset/delete the PostgreSQL database. MFA is explicitly deferred until the final security phase; keep development login email+password only. Do not reintroduce `procurement_requisitions`; the canonical table is `purchase_requisitions`. Before any new module, inspect the current project, run syntax/preflight checks, preserve all existing modules, audit every API/UI connection, and only package a ZIP after a full regression audit. Production email remains deferred until the owner has a verified domain/sender and Resend API key. The next work should start only after live Render testing of the 1–20 hardening build.
