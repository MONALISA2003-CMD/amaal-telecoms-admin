import fs from 'node:fs';import path from 'node:path';
const root=process.cwd(), pub=path.join(root,'apps/public-web'), admin=path.join(root,'apps/business-admin');const files=['lib/computer-catalogue.ts','lib/category-navigation.ts','components/ComputerCatalogueClient.tsx','app/categories/computers/laptops/[slug]/page.tsx','app/categories/computers/laptops/brand/[brand]/page.tsx','app/categories/[...path]/page.tsx'];const failures=files.filter(f=>!fs.existsSync(path.join(pub,f))).map(f=>`Missing ${f}`);const ts=fs.readFileSync(path.join(pub,'lib/computer-catalogue.ts'),'utf8');const slugs=[...ts.matchAll(/slug:'([^']+)'/g)].map(m=>m[1]);if(new Set(slugs).size!==slugs.length) failures.push('Duplicate computer slugs');const starter=fs.readFileSync(path.join(admin,'data/starter-catalogue.ts'),'utf8');for(const x of ['Computers','Computers / Laptops / HP','Computers / Laptops / Lenovo','Computers / Laptops / Apple'])if(!starter.includes(`'${x}'`))failures.push(`Missing admin category ${x}`);const seed=fs.readFileSync(path.join(root,'computer-catalogue-seed.sql'),'utf8');if(/DROP\s|TRUNCATE\s|DELETE\s+FROM/i.test(seed))failures.push('Computer seed contains destructive SQL');if(failures.length){console.error('COMPUTER AUDIT FAILED');failures.forEach(x=>console.error('- '+x));process.exit(1)}console.log(`COMPUTER AUDIT PASS — ${slugs.length} unique computer definitions, admin taxonomy, public routes and non-destructive seed present.`);
