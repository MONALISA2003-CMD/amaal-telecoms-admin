import SiteHeader from '../../components/SiteHeader';import SiteFooter from '../../components/SiteFooter';import ProductCard from '../../components/ProductCard';import {getCatalog} from '../../lib/catalog';
export default async function ShopPage(){const c=await getCatalog();const products=c?.products??[];return <main><SiteHeader/><section className="section listing-page"><div className="listing-intro"><p className="eyebrow">THE COLLECTION</p><h1>Shop Amaal.</h1><p>Explore genuine consumer electronics and appliances across our collection.</p></div><div className="listing-toolbar"><span>{products.length} published products</span></div><div className="product-grid">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div>{!products.length&&<div className="empty">No published products are available yet.</div>}</section><SiteFooter/></main>}