import Link from 'next/link';import SiteHeader from '../../../components/SiteHeader';import SiteFooter from '../../../components/SiteFooter';import ProductCard from '../../../components/ProductCard';import {getCatalog} from '../../../lib/catalog';
export default async function BrandPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=await getCatalog();const b=c?.brands.find(x=>x.slug===slug);const products=(c?.products??[]).filter(p=>p.brand_slug===slug);const name=b?.name??slug.replace(/-/g,' ').replace(/\b\w/g,c=>c.toUpperCase());return <main><SiteHeader/><section className="section listing-page"><p className="eyebrow">BRAND</p><h1>{name}.</h1><p>{b?.description||`Explore ${name} products available from Amaal.`}</p><div className="listing-toolbar"><span>{products.length} published products</span><Link href="/brands">All brands</Link></div><div className="product-grid">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div>{!products.length&&<div className="empty">No published products are currently listed for this brand.</div>}</section><SiteFooter/></main>}
