import SiteHeader from '../../components/SiteHeader';import SiteFooter from '../../components/SiteFooter';import {getCatalog} from '../../lib/catalog';import Link from 'next/link';
export default async function Brands(){const c=await getCatalog();const bs=c?.brands??[];return <main><SiteHeader/><section className="section info-page"><p className="eyebrow">TRUSTED NAMES</p><h1>The brands you know.</h1><p className="info-lead">Amaal brings together technology and appliances from brands customers know and trust.</p><div className="brand-row">{bs.map(b=><Link className="brand-card" href={`/brands/${b.slug}`} key={b.id}>{b.name}</Link>)}</div>{!bs.length&&<div className="brand-row"><span className="brand-card">Apple</span><span className="brand-card">Samsung</span><span className="brand-card">TCL</span><span className="brand-card">Hisense</span><span className="brand-card">HP</span><span className="brand-card">JBL</span></div>}</section><SiteFooter/></main>}
