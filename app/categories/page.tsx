import Link from 'next/link';import SiteHeader from '../../components/SiteHeader';import SiteFooter from '../../components/SiteFooter';import {getCatalog} from '../../lib/catalog';
export default async function CategoriesPage(){const c=await getCatalog();const categories=c?.categories??[];return <main><SiteHeader/><section className="section listing-page"><p className="eyebrow">DISCOVER</p><h1>Shop by category.</h1><div className="category-grid premium-grid category-page-grid">{categories.length?categories.map(x=><Link className="category-card" href={`/categories/${x.slug}`} key={x.id}><div><h3>{x.name}</h3><p>{x.description||'Explore the collection'}</p></div></Link>):['Phones','TVs','Home Appliances','Kitchen Appliances','Computing','Sound & Speakers'].map((x,i)=><Link className="category-card" href={`/categories/${x.toLowerCase().replace(/[^a-z]+/g,'-')}`} key={x}><div><h3>{x}</h3><p>Explore the collection</p></div></Link>)}</div></section><SiteFooter/></main>}